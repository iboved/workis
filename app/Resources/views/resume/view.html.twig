{% extends "base.html.twig" %}

{% block title %}{{ resume.jobTitle }}{% endblock %}

{% block container %}
    <div class="col-xs-12 col-sm-12">
        <div class="row">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <div class="pull-right">
                        <a href="{{ path('app_resume_edit', {'id': resume.id}) }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                        <a href="{{ path('app_resume_delete', {'id': resume.id}) }}"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a>
                    </div>
                    <p class="panel-title" style="font-size: x-large; text-align: center">{{ resume.jobTitle }}</p>
                </div>
                <div class="panel-body">
                    <div class="col-xs-6 col-lg-6">
                        <div class="panel panel-default">
                            {#<div class="panel-heading">#}
                            {#<p class="panel-title" style="font-size: large; text-align: center">Summary</p>#}
                            {#</div>#}

                            {#<div class="alert alert-info" role="alert" style="text-align: center; font-size: large">Summary</div>#}
                            <div class="panel-body">
                                <p style="font-size: 20px; text-align: center">{{ 'resume.summary'|trans }}</p>
                                <hr>
                                <p style="font-size: 17px">{{ 'resume.name'|trans }}: {{ resume.name }} ({{ resume.birthday|date('d.m.Y') }})</p>
                                <p style="font-size: 17px">{{ 'resume.city'|trans }}: {{ resume.city }}</p>
                                <p style="font-size: 17px">{{ 'resume.education'|trans }}: {{ resume.education }}</p>
                                <p style="font-size: 17px">{{ 'resume.experience'|trans }}: {{ resume.experience }}</p>
                                <p style="font-size: 17px">{{ 'resume.employment'|trans }}: {{ resume.employment }}</p>
                                <hr>
                                <table class="table table-hover">
                                    <caption>{{ 'resume.contact'|trans }}</caption>
                                    <thead>
                                    <tr>
                                        <th>{{ 'resume.name'|trans }}</th>
                                        <th>Email</th>
                                        <th>{{ 'resume.phone'|trans }}</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                        <td>{{ resume.name }}</td>
                                        <td>{{ resume.email }}</td>
                                        <td>{{ resume.phone }}</td>
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div><!--/.col-xs-6 col-lg-6-->
                    <div class="col-xs-6 col-lg-6">
                        <div class="panel panel-default">
                            {#<div class="panel-heading">#}
                            {#<p class="panel-title" style="font-size: large; text-align: center">Job description</p>#}
                            {#</div>#}
                            <div class="panel-body">
                                <p style="font-size: 20px; text-align: center">{{ 'resume.skills'|trans }}</p>
                                <hr>
                                <p style="font-size: 17px">{{ resume.skills }}</p>
                            </div>
                        </div>
                    </div><!--/.col-xs-6 col-lg-6-->
                    <div class="pull-right">
                        <p class="text-muted">{{ 'resume.created'|trans }}: {{ resume.createdAt|date }}</p>
                    </div>
                </div>
            </div>
        </div><!--/row-->
    </div><!--/.col-xs-12.col-sm-12-->
{% endblock %}
